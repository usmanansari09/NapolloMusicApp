import React from 'react';

import Svg, {Defs, Path} from 'react-native-svg';

import Country_Icons from '../../../assests/Icons/country';

const Country_Icon = ({width, height}) => {
  return <Country_Icons width={width} height={height} />;
};

export default Country_Icon;
