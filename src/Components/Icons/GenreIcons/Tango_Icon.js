import React from 'react';

import Svg, {Defs, Path} from 'react-native-svg';

import Tango_Icons from '../../../assests/Icons/tango';

const Tango_Icon = ({width, height}) => {
  return <Tango_Icons width={width} height={height} />;
};

export default Tango_Icon;
