import React from 'react';

import Svg, {Defs, Path} from 'react-native-svg';

import Vocal_Icons from '../../../assests/Icons/vocal';

const Vocal_Icon = ({width, height}) => {
  return <Vocal_Icons width={width} height={height} />;
};

export default Vocal_Icon;
