import React from 'react';

import Svg, {Defs, Path} from 'react-native-svg';

import ClassicalIcons from '../../../assests/Icons/clasical';

const Classical_Icon = ({width, height}) => {
  return <ClassicalIcons width={width} height={height} />;
};

export default Classical_Icon;
