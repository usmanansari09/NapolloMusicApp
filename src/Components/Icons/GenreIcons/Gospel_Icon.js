import React from 'react';

import Svg, {Defs, Path} from 'react-native-svg';

import Gospel_Icons from '../../../assests/Icons/gospel';

const Gospel_Icon = ({width, height}) => {
  return <Gospel_Icons width={width} height={height} />;
};

export default Gospel_Icon;
