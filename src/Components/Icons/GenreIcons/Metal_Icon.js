import React from 'react';

import Svg, {Defs, Path} from 'react-native-svg';

import Metal_Icons from '../../../assests/Icons/metal';

const Metal_Icon = ({width, height}) => {
  return <Metal_Icons width={width} height={height} />;
};

export default Metal_Icon;
