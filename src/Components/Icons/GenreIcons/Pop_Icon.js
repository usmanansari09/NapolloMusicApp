import React from 'react';

import Svg, {Defs, Path} from 'react-native-svg';

import Pop_Icons from '../../../assests/Icons/pop';

const Pop_Icon = ({width, height}) => {
  return <Pop_Icons width={width} height={height} />;
};

export default Pop_Icon;
