import React from 'react';

import Svg, {Defs, Path} from 'react-native-svg';

import Jazz_Icons from '../../../assests/Icons/jazz';

const Jazz_Icon = ({width, height}) => {
  return <Jazz_Icons width={width} height={height} />;
};

export default Jazz_Icon;
