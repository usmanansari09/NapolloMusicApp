import React from 'react';

import Svg, {Defs, Path} from 'react-native-svg';

import Rock_Icons from '../../../assests/Icons/rock';

const Rock_Icon = ({width, height}) => {
  return <Rock_Icons width={width} height={height} />;
};

export default Rock_Icon;
