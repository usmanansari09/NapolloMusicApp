import React from 'react';

import Svg, {Defs, Path} from 'react-native-svg';

import Exercise_Icons from '../../../assests/Icons/excercise';

const Exercise_Icon = ({width, height}) => {
  return <Exercise_Icons width={width} height={height} />;
};

export default Exercise_Icon;
